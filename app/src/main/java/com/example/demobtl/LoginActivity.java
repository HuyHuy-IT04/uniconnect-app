package com.example.demobtl;

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;

// *Ghi chú: Đây là file tạo sẵn để biên dịch.
// Logic Đăng nhập chi tiết sẽ được phát triển ở bước sau.*

public class LoginActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Tạm thời sử dụng layout activity_register để kiểm tra
        // Sau đó sẽ thay thế bằng R.layout.activity_login
        setContentView(R.layout.activity_register);
    }
}